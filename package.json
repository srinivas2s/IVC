{
  "name": "s_ivc",
  "version": "1.0.0",
  "description": "Modern website for Innovation & Value Creation Club.",
  "main": "index.js",
  "scripts": {
    "dev": "node dev.js",
    "build": "npm install --prefix client --legacy-peer-deps && npm install --prefix server --legacy-peer-deps && npm run build --prefix client && cp -r client/dist ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}